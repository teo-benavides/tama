export num amount 10
export num spread 90
export num speed_ 200
export str example_string abs

main
    repeat
        act shots
        wait 0.5

act shots
    repeat amount i
        fire
            dir aim (2*i - 1 - amount) * spread / (2 * max(amount - 1, 1))
            spd speed_
